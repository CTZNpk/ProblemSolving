#include <algorithm>
#include <bits/stdc++.h>
#include <climits>
#include <tuple>

using namespace std;

#define ll long long
#define vll vector<ll>
#define vi vector<int>
#define all(v) v.begin(), v.end()

void sol() {
  ll n, m, L;
  cin >> n >> m >> L;
  vi hurdSt;
  vi hurdEd;
  for (int i = 0; i < n; i++) {
    int l, r;
    cin >> l >> r;
    hurdSt.push_back(l);
    hurdEd.push_back(r);
  }
}

int main(int argc, char *argv[]) {
  int tt;
  cin >> tt;
  while (tt--) {
    sol();
  }

  return 0;
}
