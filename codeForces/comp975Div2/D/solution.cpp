
#include <bits/stdc++.h>

using namespace std;

void sol() {
  int n;
  cin >> n;
  vector<int> a(n);
  for (auto &i : a) {
    cin >> i;
  }
  vector<vector<vector<int>>> dp(n, vector<vector<int>>(n, vector<int>(n, -1)));

  auto f = [&](int l, int r, int time, auto &&f)
}
int main(int argc, char *argv[]) {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int tt;

  cin >> tt;

  while (tt--) {
    sol();
  }

  return 0;
}
