#include <bits/stdc++.h>

using namespace std;

#define ll long long
#define vi vector<int>
#define vll vector<ll>
#define all(v) v.begin(), v.end()

void sol() {
  string s;
  cin >> s;
  int n = s.length();

  auto check = [&](int i1, int i2, auto &&self) {

  };

  check(0, 0, check);
  cout << "NO\n";
}

int main(int argc, char *argv[]) {
  int tt;
  cin >> tt;
  while (tt--) {
    sol();
  }
  return 0;
}
